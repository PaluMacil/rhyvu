package ry

/* Page should contain a layout and (is layout an object or just content?) should
   have a slice to store widgets. It might need to have something to specify nav &
   menu configuration. */
